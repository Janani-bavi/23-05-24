modify_array(arr, insert_index, delete_index, insert_num):
    print("Original array:", array)
    arr.insert(insert_index, insert_num)
    del arr[delete_index]
    print("Modified array:", arr)
if name == "main"
    array = list(map(int, input("Enter array elements separated by space: ").split()))
    insert_index = int(input("Enter index to insert: "))
    delete_index = int(input("Enter index to delete: "))
    insert_num = int(input("Enter number to insert: "))
    print("Original array:", array)
    array.insert(insert_index, insert_num)
    del array[delete_index]
    print("Modified array:", array)
